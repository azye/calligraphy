{
  "name": "caligraphy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "format": "npx prettier --write .",
    "preview": "vite preview --port 8080"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vite-plugin-pwa": "^0.16.4"
  },
  "dependencies": {
    "konva": "^9.2.0"
  }
}
